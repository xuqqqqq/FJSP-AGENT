[project]
name = "fjsp-harness-agent"
version = "0.1.0"
description = "Independent harness engineering agent for FJSP algorithm self-evolution."
requires-python = ">=3.10"
dependencies = [
  "langgraph>=1.0",
  "ortools==9.15.6755",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
